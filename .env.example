# Argus Sensor — environment variables
# Copy to .env and adjust for your environment:
#   cp .env.example .env

INTERFACE=ens160      # Network interface to capture (default: any)
PORT=562              # Radium public port for clients (default: 562)
SENSOR_PORT=561       # Argus internal port, loopback only (default: 561)
